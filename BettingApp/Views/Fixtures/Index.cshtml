@using BettingApp.Controllers
@model IEnumerable<Common.Model.FixtureModel>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HomeTeam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AwayTeam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.KickOff)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HomeTeam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AwayTeam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KickOff)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", "FixtureDetail" , new { id =item.ID },null) |
            @Html.ActionLink("Details", "GetFixture", "FixtureDetail", new { id = item.ID/* id=item.PrimaryKey */ },null) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
